%% [nojambox] withholds \jambox, as it does upstream.  Without the option
%% honoured, this compiles and the note is set; with it, \jambox is not
%% defined at all and TeX says so.  See EXPECT_ERROR.
\documentclass[11pt]{article}
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
\else
  \usepackage{fontspec}
\fi
\usepackage[langsci,nojambox]{linguexx}
\pagestyle{empty}
\begin{document}
\ea NOJAM an example with a note it cannot have.\jambox{(Greek)}
\z
\end{document}
