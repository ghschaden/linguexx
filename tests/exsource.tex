\input{_preamble}
\begin{document}
\ex. Short one. \exsource{(SRCINLINE 1999)}

\ex. This example sentence is deliberately long enough that the attribution
cannot possibly fit on the same line and must be pushed onto a line of its
own, where it has to be set flush right against the margin.
\exsource{(SRCFALLBACK 1869)}
\end{document}
