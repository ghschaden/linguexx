%% \eas opens a top-level example boxed so that it cannot break across a
%% page; like \eal it has no nested meaning here or upstream, so a nested one
%% says so instead of guessing at a semantics.  See EXPECT_ERROR.
\input{_preamble-langsci}
\begin{document}
\ea EASNESTONE an example.
\eas EASNESTTWO a boxed one inside it.
\zs
\z
\end{document}
